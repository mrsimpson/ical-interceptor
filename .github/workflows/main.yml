name: CI

on: [push]

jobs:
  prepare:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: install dependencies
      run: npm i
    
  test:

    runs-on: ubuntu-latest
    
    needs: [prepare]

    steps:
    - name: test
      run: npm test
